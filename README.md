# eliza-bot

A simple implementation of the ELIZA chatbot in Python

## Tests

Run unit tests with unittest discovery:

```bash
python3 -m unittest -v
```

Or run the specific test file directly:

```bash
python3 tests/test_eliza.py
```
